spring:
  application:
    name: stats-server # ??? stats-server
  cloud:
    config:
      discovery:
        enabled: true # ???????????? Eureka ??? ?????? Config Server
        service-id: config-server # ??? ??????? Config Server ? Eureka
      fail-fast: true # ?????????? ??????, ???? ?????? ??????????

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${random.value}